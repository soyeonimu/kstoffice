name: Notion to Jekyll (pirafrank)

on:
  workflow_dispatch:
  schedule:
    - cron: "0 */6 * * *"

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Notion to Jekyll
        id: notion_to_jekyll
        uses: pirafrank/notion-to-jekyll@v1
        with:
          notion-token: ${{ secrets.NOTION_TOKEN }}
          notion-database-id: ${{ secrets.NOTION_DATABASE_ID }}
          publish-to-posts: "true"
          posts-dir: "_board"
          relative-date: "0"
          site-url: "https://soyeonimu.github.io/kstoffice"
          assets-dir: "assets/notion"

      - name: Normalize frontmatter and filename
        run: |
          python3 -m pip install --upgrade pip pyyaml
          python3 - << 'PY'
          import os, glob, re, datetime
          import yaml

          BOARD_DIR = "_board"
          KEEP = ["layout", "title", "date", "category", "description", "keywords"]

          def safe_slug(name: str) -> str:
              name = re.sub(r'\.md$', '', name, flags=re.I)
              name = re.sub(r'^\d{4}[-_]\d{2}[-_]\d{2}[-_ ]*', '', name)
              name = re.sub(r'\s+', '-', name.strip())
              name = re.sub(r'[\\/:*?"<>|]+', '', name)
              name = re.sub(r'-{2,}', '-', name)
              return name or "untitled"

          def normalize_date(raw):
              if raw is None:
                  return datetime.date.today().strftime("%Y-%m-%d")
              s = str(raw).strip()
              digits = re.sub(r'[^0-9]', '', s)[:8]
              if len(digits) == 8:
                  return f"{digits[:4]}-{digits[4:6]}-{digits[6:8]}"
              if re.match(r'^\d{4}-\d{2}-\d{2}$', s):
                  return s
              return datetime.date.today().strftime("%Y-%m-%d")

          def split_frontmatter(text):
              m = re.match(r'^---\s*\n(.*?)\n---\s*\n(.*)$', text, re.DOTALL)
              if not m:
                  return None, None
              return m.group(1), m.group(2)

          def unique_path(dir_name, filename):
              base, ext = os.path.splitext(filename)
              candidate = os.path.join(dir_name, filename)
              if not os.path.exists(candidate):
                  return candidate
              i = 2
              while True:
                  cand = os.path.join(dir_name, f"{base}-{i}{ext}")
                  if not os.path.exists(cand):
                      return cand
                  i += 1

          def process_file(path):
              dir_name = os.path.dirname(path)
              base_name = os.path.basename(path)

              with open(path, "r", encoding="utf-8") as f:
                  content = f.read()

              fm_raw, body = split_frontmatter(content)
              if fm_raw is None:
                  return

              try:
                  fm = yaml.safe_load(fm_raw) or {}
                  if not isinstance(fm, dict):
                      fm = {}
              except Exception:
                  return

              title = fm.get("title")
              if not title or str(title).strip() == "":
                  title = safe_slug(base_name).replace("-", " ")

              date = normalize_date(fm.get("date"))

              out = {
                  "layout": "board-post",
                  "title": str(title).strip(),
                  "date": date,
              }

              for k in ["category", "description", "keywords"]:
                  v = fm.get(k)
                  if v is None:
                      continue
                  if isinstance(v, str) and v.strip() == "":
                      continue
                  if isinstance(v, (list, dict)) and len(v) == 0:
                      continue
                  out[k] = v

              ordered = {k: out[k] for k in KEEP if k in out}

              new_fm = "---\n" + yaml.safe_dump(
                  ordered,
                  sort_keys=False,
                  allow_unicode=True,
                  default_flow_style=False
              ).strip() + "\n---\n"

              new_content = new_fm + body.lstrip()

              slug = safe_slug(str(title))
              new_filename = f"{date}-{slug}.md"
              target_path = unique_path(dir_name, new_filename)

              if new_content != content:
                  with open(path, "w", encoding="utf-8") as f:
                      f.write(new_content)

              if os.path.abspath(path) != os.path.abspath(target_path):
                  os.rename(path, target_path)
                  print(f"Fixed: {os.path.basename(target_path)}")

          if os.path.isdir(BOARD_DIR):
              for path in glob.glob(os.path.join(BOARD_DIR, "*.md")):
                  process_file(path)
          PY

      - name: Commit & Push
        run: |
          git config user.name "notion-bot"
          git config user.email "notion-bot@users.noreply.github.com"
          git add .
          git commit -m "sync: notion -> jekyll (normalized)" || echo "No changes to commit"
          git push
